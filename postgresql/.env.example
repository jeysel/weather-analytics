# postgresql/.env
# Copie para .env e preencha. NUNCA commite o .env real.

# ── Banco de dados ─────────────────────────────────────────────────────────
POSTGRES_DB=weather_staging
POSTGRES_USER=weather_user
POSTGRES_PASSWORD=TROQUE_ESTA_SENHA
POSTGRES_PORT=5432

# ── dbt ────────────────────────────────────────────────────────────────────
DBT_TARGET=dev
WEATHER_DBT_USER=dbt_user
WEATHER_DBT_PASSWORD=TROQUE_ESTA_SENHA
DBT_PROFILES_DIR=~/.dbt
DBT_SOURCE_DATABASE=weather_staging
DBT_SOURCE_SCHEMA=raw

# ── GCP / BigQuery ─────────────────────────────────────────────────────────
GCP_PROJECT_ID=seu-projeto-gcp
GCP_CREDENTIALS_PATH=./secrets/gcp-service-account.json
BIGQUERY_DATASET=weather_dw
BIGQUERY_LOCATION=southamerica-east1
